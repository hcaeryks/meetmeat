[project]
name = "backend"
version = "0.1.0"
description = "Group scheduling app backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
    "bcrypt",
    "pydantic",
]
